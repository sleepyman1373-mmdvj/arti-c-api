/*
 * This translation unit exists only so that CMake has a source file for
 * the shared-library target; all exported symbols come from the Rust
 * static library (libarti_ffi.a). It is intentionally empty.
 */
